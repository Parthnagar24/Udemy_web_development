const arr = [1, 2, 6];
const arr2 = arr.shift();
console.log(arr2); // Output: 1
console.log(arr); // Output: [2, 6]

// The shift method removes the first item from an array and returns that item. If the array is empty, it returns undefined.
