/*

2.What are the data types supported by JavaScript?

->
JavaScript supports several data types, which can be categorized into two main groups: 

* primitive -these include undefined, null, boolean, number, string, and symbol.

* non-primitive -these include objects, arrays, and functions.

*/

let num = 42; // number
let str = "Hello, World!"; // string
let isTrue = true; // boolean
let arr = [1, 2, 3]; // array
let obj = { key: "value" }; // object
let func = function () {
  return "I am a function";
}; // function
