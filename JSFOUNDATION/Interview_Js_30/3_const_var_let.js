/*

3.What is the difference between var, let, and const in JavaScript?

->

*var :Function-scoped , aallows redeclaration, hoisted with undefined initialization.

*let :Block-scoped, does not allow redeclaration, hoisted without initialization.

*const :Block-scoped, does not allow redeclaration, must be initialized at declaration, cannot be reassigned.

*/

var a = 10;
let b = 20;
const c = 30;
